# Custom Scalars
scalar Date

type Query {
  # Users
  users: [User]!
  user(id: ID!): User

}

type Mutation {
  # Users
  createUser(user: UserCreateInput!): User!
  updateUser(id: ID!, user: UserUpdateInput!): User!

}